goog.provide('M.style.Simple');

/**
 * TODO
 */
(function() {

  /**
   * TODO
   */
  M.style.Simple = (function(options) {
    //para que rec. options si luego no lo guarda y el const. no se hereda?
  });
  goog.inherits(M.style.Simple, M.style.Feature);


  /**
   * TODO
   * Como es protected no se rellena
   */
  M.style.Simple.prototype.apply_ = function(element) {};

  /**
   * TODO
   * hace falta poner el get de nuevo si ya lo tiene de style?
   */
  M.style.Simple.prototype.get = function(property) {};


  /**
   * TODO
   * hace falta poner el set de nuevo si ya lo tiene de style?
   */
  M.style.Simple.prototype.set = function(property, value) {};

  /**
   * TODO
   * Como es protected no se rellena
   */
  M.style.Simple.prototype.toSLD = function() {};

})();
